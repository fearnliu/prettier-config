const config = {
  // Explicitly set configuration options whose default values differ across Prettier versions.
  "arrowParens": "always",
  "endOfLine": "lf",
  "trailingComma": "all",
  // Configuration options not explicitly specified will use Prettier's default values.
  "singleAttributePerLine": true,
  "vueIndentScriptAndStyle": true,
};

export default config;
